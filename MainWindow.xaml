<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="JournalApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:JournalApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="JournalApp"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Background="Transparent" Height="48" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image Source="Assets/AppIcon.ico" Width="16" Height="16" Margin="16,0,8,0" VerticalAlignment="Center" />
            <TextBlock x:Name="AppTitleTextBlock" Text="JournalApp" Grid.Column="1" VerticalAlignment="Center" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,24,0" />
            
            <AutoSuggestBox x:Name="TitleSearchBox" Grid.Column="2" PlaceholderText="Search entries..." QueryIcon="Find" Width="300" VerticalAlignment="Center" HorizontalAlignment="Center" TextChanged="TitleSearchBox_TextChanged" Margin="0,0,140,0" />
            <Button x:Name="TitleBarUpdateBtn" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,150,0" Style="{StaticResource AccentButtonStyle}" Visibility="Collapsed" Click="TitleBarUpdateBtn_Click" Padding="12,4" CornerRadius="4">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE896;" FontSize="13" VerticalAlignment="Center" />
                    <TextBlock x:Name="TitleBarUpdateText" Text="Update Available" VerticalAlignment="Center" FontSize="12" />
                    <ProgressBar x:Name="TitleBarUpdateProgress" Width="80" Height="4" VerticalAlignment="Center" Minimum="0" Maximum="100" Visibility="Collapsed" />
                </StackPanel>
            </Button>
        </Grid>

        <!--
            The Frame hosts pages for your application content. Add your UI to
            MainPage.xaml rather than here so you can use Page features such as
            navigation events and the Loaded lifecycle.
        -->
        <Frame x:Name="RootFrame" Grid.Row="1" />
    </Grid>
</Window>
